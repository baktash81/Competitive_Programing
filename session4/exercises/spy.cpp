#include <bits/stdc++.h>
#define int long long 
using namespace std;

int32_t main(){
    int num;cin>>num;
    vector<string>coops(num);
    for(int i=0;i<num;i++){
        cin.clear();
        getline(cin,coops[i]);
    }
    int e_num;
    cin>>e_num;
    vector<string>mails(e_num);
    for(int i=0;i<e_num;i++){
        cin.clear(); 
        getline(cin,mails[i]);
    }

}